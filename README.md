Steganography Project – LSB Image Encoder & Decoder

A Python-based image steganography system that hides secret text messages inside images using the Least Significant Bit (LSB) technique. This project features simple command-line tools for encoding and decoding messages inside PNG images.

🚀 Features

Hide secret text inside an image (Encode)

Retrieve hidden text from a stego image (Decode)

Uses LSB pixel manipulation

Works with PNG images

Simple and beginner-friendly Python implementation

No external backend required

🛠️ Technologies Used

Python 3

Pillow (PIL) library

Basic CLI (Command Line Interface)
